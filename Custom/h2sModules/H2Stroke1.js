main();
function main()
{
    if(getVar("Contact1_unit", false) && getVar("Contact1_unit1", false))
    {
        Contact1_unit();
        return;
    }
    CMessage("I hope you dont mind that she will join us today %EmoteLaugh%");
    addContact(2);
    CMessage("Look whos here");
    SMessage("Hello %ShortName%!", -1, 2);
    SMessage("Hi %SubName%", -1, 2);
    SMessage("%ShortName% told me many things about you %SubName% %Grin%", -1, 2);
    SMessage("She said that you are able to stroke your %Cock% for hours %EmoteMoan%", -1, 2);
    CMessage("Did I really tell you that %Contact1%? %EmoteHer%");
    SMessage("YES you did %ShortName% %EmoteLaugh%", -1, 2);
    SMessage("Anyway I was so aroused in getting the chance to test you on my own %PetName% %EmoteBreath%", -1, 2);
    SMessage("And now we are both here. %EmoteMoan%", -1, 2);
    SMessage("Its kind of weird", -1, 2);
    SMessage("What about some warming up?", -1, 2);
    SMessage("%Edge%", -1, 2);
    startEdging();
    var amountEdges = 3;
    DoEdges(amountEdges, amountEdges, 0);
    SMessage("I never did that before.", -1, 2);
    SMessage("When %ShortName% told me about you, I couldnt get you out of my mind.", -1, 2);
    SMessage("I was thinking about you nearly every day", -1, 2);
    SMessage("Just the thought of making you edge, over and over again, gets me totally wet", -1, 2);
    SMessage("Thinking of you", -1, 2);
    SMessage("stroking your hard %Cock% at full speed for me", -1, 2);
    SMessage("moaning my name out loud, while stroking for me in your chair", -1, 2);
    SMessage("%SubName% the imagination of your dripping %Cock% makes me vibrating here", -1, 2);
    SMessage("I am all soaking wet, so lets get to the funny part", -1, 2);
    setVar("Contact1_unit", true);
    CMessage("Looks like its getting funny, so i am sneaking outta here");
    removeContact(1);
    let answer0 = getInput("%SubName% are you still dressed?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("What should i do with this answer?", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        SMessage("this will cost you at least 10 edges extra", -1, 2);
        setVar("EdgesAdd", 10);
        Sub_undress();
        return;
    }
    else if (answer0.isLike("no"))
    {
        SMessage("Thats a good boy!!! So we can move on", -1, 2);
    }
    SMessage("So please get in wanking position and", -1, 2);
    SMessage("Tell me when you are ready to get started!", -1, 2);
    SMessage("so lets start", -1, 2);
    Sub_hard();
    return;
    Contact1_unit();
}
function Contact1_unit()
{
    addContact(2);
    CMessage("Hello again %Contact1%");
    CMessage("Nice to have you back");
    SMessage("Thx %ShortName%", -1, 2);
    SMessage("I really enjoyed my last lesson with %SubName%", -1, 2);
    SMessage("He did a pretty good job " + "-)", -1, 2);
    CMessage("Thats good to hear %Contact1%, hope you will have lots of fun today with him");
    CMessage("Will take me a while to get back here, so you have nearly the whole day with him %Lol%");
    SMessage("Oh that sounds good to me %ShortName%", -1, 2);
    SMessage("I havent planned anything else, than making %PetName% edge %EmoteLaugh%", -1, 2);
    CMessage("Thats a great idea, so i am already out of here, bye bye");
    removeContact(1);
    SMessage("Speaking of edges", -1, 2);
    SMessage("For warmup give me some edge", -1, 2);
    startEdging();
    var amountEdges = 4;
    DoEdges(amountEdges, amountEdges, 0);
    SMessage("I am sure we will have fun again", -1, 2);
    setVar("Contact1_unit1", true);
    let answer0 = getInput("%SubName% are you still dressed?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("What should i do with this answer?", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        SMessage("this will cost you at least 10 edges extra", -1, 2);
        setVar("EdgesAdd", 10);
        Sub_undress();
        return;
    }
    else if (answer0.isLike("no"))
    {
        SMessage("Thats a good boy!!! So we can move on", -1, 2);
    }
    SMessage("So please get in wanking position", -1, 2);
    let answer1 = getInput("Tell me when you are ready to get started!", -1, true, 2);
    if (answer1.isLike("ready", "ok", "yes"))
    {
        SMessage("so lets start", -1, 2);
        Sub_hard();
        return;
    }
    Contact1_unit1();
}
function Contact1_unit1()
{
    addContact(2);
    CMessage("Hey %Contact1%, nice to see you");
    CMessage("How are you today?");
    SMessage("Thank %ShortName%, everything is fine.", -1, 2);
    SMessage("And what about you?", -1, 2);
    CMessage("Also fine, and thank you, that you take care of %SubName% again.");
    CMessage("I have a lot of things to get done, so i will just fly and leave you all alone.");
    CMessage("We will chat later, when i get back. See you.");
    SMessage("Ok %ShortName%, see you later", -1, 2);
    removeContact(1);
    SMessage("Now we are all alone again %SubName%.", -1, 2);
    SMessage("I really enjoy making you squirm and ache for me %PetName%", -1, 2);
    SMessage("Now i understand what %ShortName% likes that much on you.", -1, 2);
    SMessage("It is really funny to see your %Cock% %Ache%.", -1, 2);
    SMessage("But as you know, to get an aching %Cock%, you need to %Stroke% a lot.", -1, 2);
    SMessage("And i am sure you that you already realised, that this is going to happen right now %Smile%", -1, 2);
    SMessage("The only things that needs to be cleared are", -1, 2);
    let answer0 = getInput("Naked", -1, true, 2);
    while (!(answer0.isLike("no") || answer0.isLike("yes")))
    {
        answer0 = getInput("try again");
    }
    if (answer0.isLike("no"))
    {
        SMessage("You know what that means, dont you? 20 edges extra", -1, 2);
        setVar("EdgesAdd", 20);
    }
    else if (answer0.isLike("yes"))
    {
        SMessage("Well prepared %PetName%", -1, 2);
    }
    SMessage("And the second thing is", -1, 2);
    let answer1 = getInput("Hard", -1, true, 2);
    while (!(answer1.isLike("no") || answer1.isLike("yes")))
    {
        answer1 = getInput("Try again");
    }
    if (answer1.isLike("no"))
    {
        SMessage("Do i really need to tell you again? Another 20 edges extra", -1, 2);
        Sub_hard();
        return;
    }
    else if (answer1.isLike("yes"))
    {
        SMessage("Well prepared %PetName%", -1, 2);
    }
    SMessage("I think we can get things going now", -1, 2);
    wait(30);
    Sub_decision();
    return;
    Sub_undress();
}
function Sub_undress()
{
    SMessage("Ok then, lets get rid of this useless clothes.", -1, 2);
    SMessage("I want you bare naked and ready to serve.", -1, 2);
    SMessage("I hope i dont need to tell you that you just have 1 minute to be ready", -1, 2);
    wait(40);
    SMessage("AWWW too bad, you didnt make it in time, which gives another 20 edges extra", -1, 2);
    setVar("EdgesAdd", getVar("EdgesAdd", 0) + 20);
    SMessage("Now you should be naked and sitting in your chair.", -1, 2);
    Sub_hard();
}
function Sub_hard()
{
    let answer0 = getInput("but is your cock hard already", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("What should i do with this answer?", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        SMessage("OK then stand up and present your %Cock% to me now and be ready to worship me", -1, 2);
        Sub_decision();
        return;
    }
    else if (answer0.isLike("no"))
    {
        SMessage("this brings up another 20 edges extra", -1, 2);
    }
    EdgesAdd();
}
function EdgesAdd()
{
    SMessage("Seems you picked up some extra work", -1, 2);
    setVar("EdgesAdd", getVar("EdgesAdd", 0) + 20);
    SMessage("Fine %EmoteLaugh%", -1, 2);
    let answer0 = getInput("Do you need some help to get hard?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("Try again");
    }
    if (answer0.isLike("yes"))
    {
        Sub_help();
        return;
    }
    else if (answer0.isLike("no"))
    {
        Sub_decision();
        return;
    }
    Sub_help();
}
function Sub_help()
{
    SMessage("I hope this video will help to get your %Cock% hard", -1, 2);
    playVideo("Videos" + java.io.File.separator + "*.*");
    SMessage("I expect that you are hard now so", -1, 2);
    let answer0 = getInput("Tell me when you are ready to get to serious stroking!", -1, true, 2);
    while (!(answer0.isLike("ready") || answer0.isLike("ok") || answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("???", null, null, true, 2);
    }
    if (answer0.isLike("ready", "ok", "yes"))
    {
        SMessage("Ok then lets get to work", -1, 2);
        Sub_decision();
        return;
    }
    else if (answer0.isLike("no"))
    {
        SMessage("Then you get another chance", -1, 2);
        Sub_help();
        return;
    }
    Sub_decision();
}
function Sub_decision()
{
    SMessage("So what are we going to do today %SubName%?", -1, 2);
    SMessage("What if I let decide YOU", -1, 2);
    SMessage("I think I will give you 3 options", -1, 2);
    SMessage("Some edges?", -1, 2);
    SMessage("A few ruined orgasms?", -1, 2);
    SMessage("Or CBT and ballbusting?", -1, 2);
    let answer0 = getInput("So whats your favourite?", -1, true, 2);
    while (!(answer0.isLike("edges") || answer0.isLike("1") || answer0.isLike("ruin") || answer0.isLike("2") || answer0.isLike("CBT") || answer0.isLike("3")))
    {
        answer0 = getInput("Thats not what I wanted to hear, try again");
    }
    if (answer0.isLike("edges", "1"))
    {
        Sub_edges();
        return;
    }
    else if (answer0.isLike("ruin", "2"))
    {
        Sub_ruin();
        return;
    }
    else if (answer0.isLike("CBT", "3"))
    {
        Sub_cbt();
        return;
    }
    Sub_edges();
}
function Sub_edges()
{
    SMessage("Thats what I like on you %SubName%", -1, 2);
    SMessage("Your unbroken will to serve me with all that edges", -1, 2);
    SMessage("So lets find out what we have to deal with " + "-)", -1, 2);
    SMessage("For this purpose I have brought a special dice %Lol%", -1, 2);
    SMessage("A 20-sided dice, which I am going to use for this", -1, 2);
    setVar("Sub_EdgeGoal", randomInteger(1, 20));
    SMessage("The dice tells me " + getVar("Sub_EdgeGoal"), -1, 2);
    SMessage("So far, so good", -1, 2);
    SMessage("And now let the fate decide the multiplier for us %Lol%", -1, 2);
    if (getVar("Sub_EdgeGoal", 0) <= 5)
    {
        Change_var();
        return;
    }
    SMessage("Its not really what I wanted, but I have to deal with it", -1, 2);
    ExtraEdges();
    return;
    Change_var();
}
function Change_var()
{
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) * randomInteger(2, 3));
    SMessage("So fate decided", -1, 2);
    SMessage("that you have to do "+ getVar("Sub_EdgeGoal") + "edges for me tonight %PetName%", -1, 2);
    CMessage("edges for me tonight %PetName%");
    ExtraEdges();
}
function ExtraEdges()
{
    SMessage("Lets have a look, if you had some extra edges :-)", -1, 2);
    Smessage(getVar("EdgesAdd"), -1, 2);
    SMessage("As you remember, you were not really prepared and picked up some extra edges", -1, 2);
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) + getVar("EdgesAdd", 0));
    SMessage("So we end up with " + getVar("Sub_EdgeGoal"), -1, 2);
    CMessage("in total for %GeneralTime%");
    SMessage("Sounds good to me %SubName%", -1, 2);
    let answer0 = getInput("But what about you?", -1, true, 2);
    while (!(answer0.isLike("ok") || answer0.isLike("good") || answer0.isLike("fine") || answer0.isLike("good") || answer0.isLike("much") || answer0.isLike("good") || answer0.isLike("like") || answer0.isLike("no")))
    {
        answer0 = getInput("...");
    }
    if (answer0.isLike("ok", "good", "fine"))
    {
        SMessage("Just what I have thought " + "-)", -1, 2);
        Start_Session();
        return;
    }
    else if (answer0.isLike("not good", "too much", "no good", "dont like", "no"))
    {
        SMessage("Did i really hear that?", -1, 2);
    }
    SMessage("So you think thats too much for you?", -1, 2);
    SMessage("%Edge%", -1, 2);
    startEdging();
    var amountEdges = 2;
    DoEdges(amountEdges, amountEdges, 0);
    SMessage("Hmmm in fact that you dont want to do that much edges for me, i think it is necessary to explain you, who makes the rules here.", -1, 2);
    SMessage("And that you have to learn it from the beginning, i will not be that easy on you this time", -1, 2);
    setVar("Sub_EdgesAdd", randomInteger(10, 15));
    SMessage("I decided that i increase your edge-account with " + getVar("Sub_EdgesAdd") + "edges", -1, 2);
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) + getVar("Sub_EdgesAdd", 0));
    SMessage("Next time i think i will not be that kind to you", -1, 2);
    SMessage("Which means you have to do " + getVar("Sub_EdgeGoal") + "edges for me now!", -1, 2);
    SMessage("And be sure that you will do them all!", -1, 2);
    SMessage("So you better think of your answers before!", -1, 2);
    Start_Session();
}
function Start_Session()
{
    SMessage("Seems we have some serious work to do for YOU %Grin%", -1, 2);
    SMessage("What are we waiting for?", -1, 2);
    SMessage("I\'m going to show you some pictures I found online", -1, 2);
    setVar("Edge_Count", 0);
    SMessage("And when I think I have a really good one", -1, 2);
    SMessage("I\'ll let you edge to it %Grin%", -1, 2);
    SMessage("When you reached your Edge goal, we can move on", -1, 2);
    setTempVar("av_intera_017", true);
    SMessage("Get ready %PetName%", -1, 2);
    lockImages();
    Show_Image();
}
function Show_Image()
{
    getTeasePicture();
    if (randomInteger(1, 100) <= 75)
    {
        Show_Image();
        return;
    }
    SMessage("I want you to edge for this one %PetName%", -1, 2);
    SMessage("Get close for me right now", 0, 2);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    SMessage("Just sit back and relax", -1, 2);
    unlockImages();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Edge_Count", 0) == getVar("Sub_EdgeGoal", 0))
    {
        Edge_Goal_Met();
        return;
    }
    SMessage("Let yourself calm down as much as you can", -1, 2);
    setVar("EdgeRemaining", getVar("Sub_EdgeGoal", 0) - getVar("Edge_Count", 0));
    SMessage("You still have " + getVar("EdgeRemaining") + "edges to go %Grin%", -1, 2);
    switch(random("Rocket_man", "Edge_loop", "Show_Image"))
    {
        case "Rocket_man":
        Rocket_man();
        return;
        break;
        case "Edge_loop":
        Edge_loop();
        return;
        break;
        case "Show_Image":
        Show_Image();
        return;
        break;
    }
    Rocket_man();
}
function Rocket_man()
{
    SMessage("Lets make a little exciting countdown to get the rocket up", -1, 2);
    SMessage("%SubName%, edge on 0", -1, 2);
    setRapidText(true);
    SMessage("10", -1, 2);
    SMessage("9", -1, 2);
    SMessage("8", -1, 2);
    SMessage("7", -1, 2);
    SMessage("6", -1, 2);
    SMessage("5", -1, 2);
    SMessage("4", -1, 2);
    SMessage("3", -1, 2);
    SMessage("2", -1, 2);
    SMessage("1", -1, 2);
    SMessage("EDGE!", -1, 2);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    setRapidText(false);
    if (getVar("Edge_Count", 0) == getVar("Sub_EdgeGoal", 0))
    {
        Edge_Goal_Met();
        return;
    }
    SMessage(getVar("Sub_EdgeGoal", "") + "dripping edges to go for you %SubName%", -1, 2);
    switch(random("Rocket_man", "Edge_loop", "Show_Image", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3", "Loop_no_hold4", "Loop_no_hold5"))
    {
        case "Rocket_man":
        Rocket_man();
        return;
        break;
        case "Edge_loop":
        Edge_loop();
        return;
        break;
        case "Show_Image":
        Show_Image();
        return;
        break;
        case "Loop_no_hold":
        Loop_no_hold();
        return;
        break;
        case "Loop_no_hold1":
        Loop_no_hold1();
        return;
        break;
        case "Loop_no_hold2":
        Loop_no_hold2();
        return;
        break;
        case "Loop_no_hold3":
        Loop_no_hold3();
        return;
        break;
        case "Loop_no_hold4":
        Loop_no_hold4();
        return;
        break;
        case "Loop_no_hold5":
        Loop_no_hold5();
        return;
        break;
    }
    Edge_loop();
}
function Edge_loop()
{
    CMessage("Still ok for you?");
    let answer0 = getInput("Do you need a break?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("..", null, null, true, 2);
    }
    if (answer0.isLike("yes"))
    {
        SMessage("Then take a short break", -1, 2);
        setRapidText(true);
        Break();
        return;
    }
    else if (answer0.isLike("no"))
    {
        SMessage("I hope, you know what you are doing", -1, 2);
        setRapidText(true);
        switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3", "Loop_no_hold4", "Loop_no_hold5"))
        {
            case "Rocket_man":
            Rocket_man();
            return;
            break;
            case "Loop_no_hold":
            Loop_no_hold();
            return;
            break;
            case "Loop_no_hold1":
            Loop_no_hold1();
            return;
            break;
            case "Loop_no_hold2":
            Loop_no_hold2();
            return;
            break;
            case "Loop_no_hold3":
            Loop_no_hold3();
            return;
            break;
            case "Loop_no_hold4":
            Loop_no_hold4();
            return;
            break;
            case "Loop_no_hold5":
            Loop_no_hold5();
            return;
            break;
        }
    }
    Loop_no_hold();
}
function Loop_no_hold()
{
    setRapidText(false);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Sub_EdgeGoal", 0) == 0)
    {
        Edge_Goal_Met();
        return;
    }
    let answer0 = getInput("Wanna try some more?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("Thats fine", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        setRapidText(true);
        switch(random("Rocket_man", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3", "Loop_no_hold4", "Loop_no_hold5"))
        {
            case "Rocket_man":
            Rocket_man();
            return;
            break;
            case "Loop_no_hold1":
            Loop_no_hold1();
            return;
            break;
            case "Loop_no_hold2":
            Loop_no_hold2();
            return;
            break;
            case "Loop_no_hold3":
            Loop_no_hold3();
            return;
            break;
            case "Loop_no_hold4":
            Loop_no_hold4();
            return;
            break;
            case "Loop_no_hold5":
            Loop_no_hold5();
            return;
            break;
        }
    }
    else if (answer0.isLike("no"))
    {
        CMessage("%stopstroking%", 0);
        setRapidText(true);
        Break();
        return;
    }
    Loop_no_hold1();
}
function Loop_no_hold1()
{
    setRapidText(false);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Sub_EdgeGoal", 0) == 0)
    {
        Edge_Goal_Met();
        return;
    }
    SMessage("Still " + getVar("Sub_EdgeGoal", "") + "edges to go", -1, 2);
    let answer0 = getInput("Another one?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("Thats fine", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        setRapidText(true);
        switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold2", "Loop_no_hold3", "Loop_no_hold4", "Loop_no_hold5"))
        {
            case "Rocket_man":
            Rocket_man();
            return;
            break;
            case "Loop_no_hold":
            Loop_no_hold();
            return;
            break;
            case "Loop_no_hold2":
            Loop_no_hold2();
            return;
            break;
            case "Loop_no_hold3":
            Loop_no_hold3();
            return;
            break;
            case "Loop_no_hold4":
            Loop_no_hold4();
            return;
            break;
            case "Loop_no_hold5":
            Loop_no_hold5();
            return;
            break;
        }
    }
    else if (answer0.isLike("no"))
    {
        CMessage("%stopstroking%", 0);
        setRapidText(true);
        Break();
        return;
    }
    Loop_no_hold2();
}
function Loop_no_hold2()
{
    setRapidText(false);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    holdEdge();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Sub_EdgeGoal", 0) == 0)
    {
        Edge_Goal_Met();
        return;
    }
    let answer0 = getInput("One more possible?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("Thats fine", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        setRapidText(true);
        switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold3", "Loop_no_hold4", "Loop_no_hold5"))
        {
            case "Rocket_man":
            Rocket_man();
            return;
            break;
            case "Loop_no_hold":
            Loop_no_hold();
            return;
            break;
            case "Loop_no_hold1":
            Loop_no_hold1();
            return;
            break;
            case "Loop_no_hold3":
            Loop_no_hold3();
            return;
            break;
            case "Loop_no_hold4":
            Loop_no_hold4();
            return;
            break;
            case "Loop_no_hold5":
            Loop_no_hold5();
            return;
            break;
        }
    }
    else if (answer0.isLike("no"))
    {
        CMessage("%stopstroking%", 0);
        setRapidText(true);
        Break();
        return;
    }
    Loop_no_hold3();
}
function Loop_no_hold3()
{
    setRapidText(false);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    holdEdge();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Sub_EdgeGoal", 0) == 0)
    {
        Edge_Goal_Met();
        return;
    }
    SMessage(getVar("Sub_EdgeGoal", "") + "edges left on the countdown " + "-)", -1, 2);
    let answer0 = getInput("Can you give me one more edge baby?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("Thats fine", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        setRapidText(true);
        switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold4", "Loop_no_hold5"))
        {
            case "Rocket_man":
            Rocket_man();
            return;
            break;
            case "Loop_no_hold":
            Loop_no_hold();
            return;
            break;
            case "Loop_no_hold1":
            Loop_no_hold1();
            return;
            break;
            case "Loop_no_hold2":
            Loop_no_hold2();
            return;
            break;
            case "Loop_no_hold4":
            Loop_no_hold4();
            return;
            break;
            case "Loop_no_hold5":
            Loop_no_hold5();
            return;
            break;
        }
    }
    else if (answer0.isLike("no"))
    {
        CMessage("%stopstroking%", 0);
        setRapidText(true);
        Break();
        return;
    }
    Loop_no_hold4();
}
function Loop_no_hold4()
{
    setRapidText(false);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Sub_EdgeGoal", 0) == 0)
    {
        Edge_Goal_Met();
        return;
    }
    SMessage("Oh baby, you look very frustrated.", -1, 2);
    let answer0 = getInput("Looks like you need a break, dont you?", -1, true, 2);
    while (!(answer0.isLike("yes") || answer0.isLike("no")))
    {
        answer0 = getInput("Thats fine", -1, 2);
    }
    if (answer0.isLike("yes"))
    {
        SMessage("I am NOT thinking that you need a break, so we simply move on with the next edge", -1, 2);
        setRapidText(true);
        switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3"))
        {
            case "Rocket_man":
            Rocket_man();
            return;
            break;
            case "Loop_no_hold":
            Loop_no_hold();
            return;
            break;
            case "Loop_no_hold1":
            Loop_no_hold1();
            return;
            break;
            case "Loop_no_hold2":
            Loop_no_hold2();
            return;
            break;
            case "Loop_no_hold3":
            Loop_no_hold3();
            return;
            break;
        }
    }
    else if (answer0.isLike("no"))
    {
        SMessage("Fine so we can still continue edging", -1, 2);
        setRapidText(true);
        switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3"))
        {
            case "Rocket_man":
            Rocket_man();
            return;
            break;
            case "Loop_no_hold":
            Loop_no_hold();
            return;
            break;
            case "Loop_no_hold1":
            Loop_no_hold1();
            return;
            break;
            case "Loop_no_hold2":
            Loop_no_hold2();
            return;
            break;
            case "Loop_no_hold3":
            Loop_no_hold3();
            return;
            break;
        }
    }
    Loop_no_hold5();
}
function Loop_no_hold5()
{
    SMessage("Another slideshow for you comming up", -1, 2);
    Boobs();
}
function Boobs()
{
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Sub_EdgeGoal", 0) == 0)
    {
        Edge_Goal_Met();
        return;
    }
    unlockImages();
    switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3", "Loop_no_hold4"))
    {
        case "Rocket_man":
        Rocket_man();
        return;
        break;
        case "Loop_no_hold":
        Loop_no_hold();
        return;
        break;
        case "Loop_no_hold1":
        Loop_no_hold1();
        return;
        break;
        case "Loop_no_hold2":
        Loop_no_hold2();
        return;
        break;
        case "Loop_no_hold3":
        Loop_no_hold3();
        return;
        break;
        case "Loop_no_hold4":
        Loop_no_hold4();
        return;
        break;
    }
    Butt();
}
function Butt()
{
    CMessage("%Edge%");
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Sub_EdgeGoal", 0) == 0)
    {
        Edge_Goal_Met();
        return;
    }
    SMessage("Nice picture by the way", -1, 2);
    unlockImages();
    switch(random("Rocket_man", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3", "Loop_no_hold4"))
    {
        case "Rocket_man":
        Rocket_man();
        return;
        break;
        case "Loop_no_hold":
        Loop_no_hold();
        return;
        break;
        case "Loop_no_hold1":
        Loop_no_hold1();
        return;
        break;
        case "Loop_no_hold2":
        Loop_no_hold2();
        return;
        break;
        case "Loop_no_hold3":
        Loop_no_hold3();
        return;
        break;
        case "Loop_no_hold4":
        Loop_no_hold4();
        return;
        break;
    }
    Break();
}
function Break()
{
    setRapidText(false);
    SMessage("Just take a break without stroking", -1, 2);
    SMessage("Only watch this video and try to relax.", -1, 2);
    SMessage("Simply watch out, that your %Cock% stays hard.", -1, 2);
    playVideo("Videos" + java.io.File.separator + "*.*");
    SMessage("Ok your break is over", -1, 2);
    SMessage("But before we move on with working on your edgegoal, just give me one extra edge %Grin%", -1, 2);
    setRapidText(false);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    switch(random("Rocket_man", "Edge_loop", "Show_Image", "Loop_no_hold", "Loop_no_hold1", "Loop_no_hold2", "Loop_no_hold3", "Loop_no_hold4"))
    {
        case "Rocket_man":
        Rocket_man();
        return;
        break;
        case "Edge_loop":
        Edge_loop();
        return;
        break;
        case "Show_Image":
        Show_Image();
        return;
        break;
        case "Loop_no_hold":
        Loop_no_hold();
        return;
        break;
        case "Loop_no_hold1":
        Loop_no_hold1();
        return;
        break;
        case "Loop_no_hold2":
        Loop_no_hold2();
        return;
        break;
        case "Loop_no_hold3":
        Loop_no_hold3();
        return;
        break;
        case "Loop_no_hold4":
        Loop_no_hold4();
        return;
        break;
    }
    Edge_Goal_Met();
}
function Edge_Goal_Met()
{
    SMessage("So you really did all those edges!", -1, 2);
    delVar("av_intera_017");
    SMessage("Now i am very impressed %SubName%", -1, 2);
    SMessage("I think thats enough for now", -1, 2);
    unlockImages();
    addContact(1);
    removeContact(2);
    CMessage("Let\'s get back to some teasing %Grin%");
    return;
    Edge_Quicky();
}
function Edge_Quicky()
{
    SMessage("Looks like we just have a quicky today %EmoteSad%", -1, 2);
    SMessage("I am sad that you were so lucky today and got just " + getVar("Sub_EdgeGoal") + "edges", -1, 2);
    SMessage("In my dreams there where numbers with three digits %EmoteSad%", -1, 2);
    SMessage("Fate is treating me hard today", -1, 2);
    SMessage("so maybe I need to be extra rough on YOU for that?", -1, 2);
    increaseAnger(4)
    SMessage("Nevertheless lets bring this to an end", -1, 2);
    SMessage("%SubName%", -1, 2);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    SMessage("I\'m going to show you some pictures I found online", -1, 2);
    setVar("Edge_Count", 0);
    SMessage("And when I think I have a really good one", -1, 2);
    setTempVar("av_intera_017", true);
    SMessage("I\'ll let you edge to it %Grin%", -1, 2);
    SMessage("When I think you\'ve edged enough, we can move on", -1, 2);
    SMessage("Get ready %PetName%", -1, 2);
    lockImages();
    Show_Image1();
}
function Show_Image1()
{
    getTeasePicture();
    if (randomInteger(1, 100) <= 75)
    {
        Show_Image1();
        return;
    }
    SMessage("I want you to edge for this one %PetName%", -1, 2);
    SMessage("Get close for me right now", -1, 2);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    SMessage("Just sit back and relax", -1, 2);
    unlockImages();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    if (getVar("Edge_Count", 0) == getVar("EdgeGoal_dice", 0))
    {
        Edge_Goal_Met1();
        return;
    }
    SMessage("Let yourself calm down as much as you can", -1, 2);
    setVar("EdgeRemaining", getVar("Sub_EdgeGoal", 0) - getVar("Edge_Count", 0));
    SMessage("You still have " + getVar("EdgeRemaining", "") + "to go %Grin%", -1, 2);
    if (randomInteger(1, 100) <= 50)
    {
        Rocket_man1();
        return;
    }
    Show_Image1();
    return;
    Rocket_man1();
}
function Rocket_man1()
{
    SMessage("Lets make a little exciting countdown to get the rocket up", -1, 2);
    SMessage("%SubName%, edge on 0", -1, 2);
    setRapidText(true);
    SMessage("10", -1, 2);
    SMessage("9", -1, 2);
    SMessage("8", -1, 2);
    SMessage("7", -1, 2);
    SMessage("6", -1, 2);
    SMessage("5", -1, 2);
    SMessage("4", -1, 2);
    SMessage("3", -1, 2);
    SMessage("2", -1, 2);
    SMessage("1", -1, 2);
    SMessage("EDGE!", -1, 2);
    playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    startEdging();
    setVar("Sub_EdgeGoal", getVar("Sub_EdgeGoal", 0) - 1);
    setRapidText(false);
    SMessage(getVar("Sub_EdgeGoal", "") + "dripping edges to go for you %SubName%", -1, 2);
    Show_Image();
    return;
    Edge_Goal_Met1();
}
function Edge_Goal_Met1()
{
    SMessage("So you really did all those edges!", -1, 2);
    delVar("av_intera_017");
    SMessage("Now i am very impressed %SubName%", -1, 2);
    SMessage("I think thats enough for now", -1, 2);
    unlockImages();
    addContact(1);
    removeContact(2);
    CMessage("Lets get back to some teasing %Grin%");
    delVar("Sub_EdgeGoal");
    return;
    Sub_ruin();
}
function Sub_ruin()
{
    SMessage("oh so you think you can give me at least 3 ruined loads? %EmoteMoan%", -1, 2);
    setVar("Cum_Count", 0);
    SMessage("Did you recognize that I was talking about MINIMUM 3 orgasms?", -1, 2);
    SMessage("So there could be 20 loads also if I use my 20 sided dice %Lol%", -1, 2);
    SMessage("No no, Dont worry, I am not using this dice this time %EmoteLaugh%", -1, 2);
    SMessage("its just the 10 sided one this time %Lol%", -1, 2);
    SMessage("So lets see how often you will have to unload now " + "-)", -1, 2);
    setVar("Sub_CumGoal", randomInteger(3, 10));
    SMessage("Your fate tells me, that you have to do " + getVar("Sub_CumGoal", "") + "loads", -1, 2);
    SMessage("Pretty ambicious, dont you think?", -1, 2);
    SMessage("thats a lot of cum you have to produce tonight", -1, 2);
    SMessage("so your %Balls% really need to be filled up a lot", -1, 2);
    SMessage("But dont worry, thats my job, %Lol%", -1, 2);
    SMessage("And I am sure that I am pretty good at that " + "-)", -1, 2);
    SMessage("dont waste time", -1, 2);
    SMessage("Lets start off with our first edge to get warmed", -1, 2);
    startEdging();
    SMessage("As you can see i am not starting off too hard with you :-)", -1, 2);
    SMessage("But from now on, things are going to get exciting %EmoteMoan%", -1, 2);
    SMessage("Actually you decided that you want this, so its not my fault %Lol%", -1, 2);
    SMessage("you know what to do when you hear the beat right?", -1, 2);
    edgeToRuin();
    if (getVar("Cum_Count", 0) == getVar("Sub_CumGoal", 0))
    {
        Cum_Goal_Met();
        return;
    }
    setVar("Sub_CumGoal", getVar("Sub_CumGoal", 0) - 1);
    SMessage(getVar("Sub_CumGoal", "") + "orgasms left to do for you %SubName%", -1, 2);
    Cum_break();
}
function Cum_break()
{
    SMessage("I will give you a short break to allow your %Balls% to get filled up a bit again.", -1, 2);
    SMessage("Therefore sit and watch the next video, but dont touch your %Cock%", -1, 2);
    SMessage("After the video, you will immediately get the chance to touch it again", -1, 2);
    playVideo("Videos" + java.io.File.separator + "*.*");
    SMessage("Here you go", -1, 2);
    edgeToRuin();
    SMessage("Just take some deep breath now", -1, 2);
    setVar("Sub_CumGoal", getVar("Sub_CumGoal", 0) - 1);
    if (getVar("Sub_CumGoal", 0) == getVar("Cum_Count", 0))
    {
        Cum_Goal_Met();
        return;
    }
    setVar("CumRemaining", getVar("Sub_CumGoal", 0) - getVar("Cum_Count", 0));
    SMessage(getVar("Sub_CumGoal", "") + "orgasms left to do for you %SubName%", -1, 2);
    Cum_break();
    return;
    Cum_Goal_Met();
}
function Cum_Goal_Met()
{
    SMessage("So you really did all that orgasms!", -1, 2);
    SMessage("I think thats enough for now", -1, 2);
    addContact(1);
    removeContact(2);
    CMessage("Lets get back to some teasing %Grin%");
    delVar("Sub_CumGoal");
    return;
    Sub_cbt();
}
function Sub_cbt()
{
    SMessage("Hmmm thats an awesome decision. I have some weird things in mind for that", -1, 2);
    run("CBT" + java.io.File.separator + "*.js");
    SMessage("I think thats enough for now", -1, 2);
    addContact(1);
    removeContact(2);
    return;
}